import styled from 'styled-components';

export const Btn = styled.button`
    padding: .8rem 1rem;
    border-radius: .5rem;
    border: none;
    font-size: 1rem;
    background: Cyan;
    cursor: pointer;

    transition: .5s;

    &:hover {
        opacity: 0.83;
    }

`;
