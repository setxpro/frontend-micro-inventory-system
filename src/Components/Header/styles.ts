import styled from 'styled-components';

export const Container = styled.div`
    height: 70px;
    width: 100vw;
    background: #283046;
    padding: 0 1rem;

    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 1rem;

`;
