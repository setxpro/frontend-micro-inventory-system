import styled from 'styled-components';

export const Container = styled.div`
    height: calc(100vh - 70px);
    background: #161D31;

    overflow: scroll;
    ::-webkit-scrollbar {
        display: none;
    }
`;
